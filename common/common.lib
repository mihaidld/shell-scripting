# common lib
# No #!/bin/sh since no extra shell spawn

STD_MSG="About to rename some files ..."

rename()
{
 # expects to be called as rename .txt .bak
 FROM=$1
 TO=$2
 echo *$FROM
 for i in *$FROM
 do
  echo "file to change is $i"
  j=`basename $i $FROM`
  mv $i ${j}$TO 
 done
}
